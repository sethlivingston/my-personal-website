<!--This SVG filter is used to "squigglify" HTML elements with the .squiggle class --> 
<svg xmlns="http://www.w3.org/2000/svg" version="1.1" height="0" width="0">
  <defs>
    <filter id="squiggle">
      <feTurbulence type="fractalNoise" id="turbulence" baseFrequency=".01" numOctaves="4" />
      <feDisplacementMap id="displacement" in="SourceGraphic" scale="5" />
    </filter>
  </defs>
</svg>
